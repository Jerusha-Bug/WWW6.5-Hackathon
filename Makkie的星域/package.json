{
  "name": "moodrealm-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for 情绪星域",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:fuji": "hardhat run scripts/deploy.js --network fuji",
    "deploy:avax": "hardhat run scripts/deploy.js --network avalanche",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^17.4.0",
    "hardhat": "^2.22.0"
  }
}
